apiVersion: apps/v1
kind: Deployment
metadata:
  name: "max"
  namespace: "agents-dev"
  labels:
    app: "max"
    team: "dev"
spec:
  replicas: 1
  selector:
    matchLabels:
      app: "max"
  template:
    metadata:
      labels:
        app: "max"
        team: "dev"
        azure.workload.identity/use: "true"
    spec:
      serviceAccountName: "sa-max"
      containers:
        - name: "max"
          image: "acrshoppoc.azurecr.io/agents/max:latest"
          ports:
            - containerPort: 8000
          envFrom:
            - configMapRef:
                name: "config-max"
          resources:
            requests:
              cpu: 250m
              memory: 512Mi
            limits:
              cpu: 500m
              memory: 1Gi
          livenessProbe:
            httpGet:
              path: /health
              port: 8000
            initialDelaySeconds: 30
            periodSeconds: 10
          readinessProbe:
            httpGet:
              path: /health
              port: 8000
            initialDelaySeconds: 10
            periodSeconds: 5
